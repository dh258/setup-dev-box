---
all:
  hosts:
    dev_server:
      ansible_connection: local # Use this for your own computer
      ansible_user: darell # Change to your username
      # ansible_host: 192.168.18.173 # Use this if you're targeting a remote instance
